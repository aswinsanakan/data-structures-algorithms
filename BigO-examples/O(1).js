// O(1) - Constant time

const boxes = new Array().fill("box");
const boxes = ["box1", "box2", "box3"];

function getFirstBox(boxes) {
  console.log(boxes(0)); // Always takes only the first box
}

getFirstBox(boxes);
